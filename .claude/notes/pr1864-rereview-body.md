## Re-review — PR #1864 @ 5ed71737f

Diff `origin/main..HEAD`; merge-base == origin/main == d912a7ee9, so the two-dot and three-dot diffs agree. 33 files, +1898/-213. Grounded against AdCP spec 3.1.1 / SDK 6.6.0, re-derived rather than assumed.

**1 BLOCKER / 12 SHOULD-FIX / 9 NIT.** 22 are posted as inline threads on their anchors; the rest are below, because their subject sits outside this diff.

The remediation is real. All three of your mutation claims reproduce here: dropping the `account_id` predicate reddens 5 tests across mcp/a2a/rest; failing the refusal open reddens 3 on all three; forcing the seam live reddens 4. The wire reads are genuine transport bytes, not re-serialized models — confirmed by mutating the harness's synthesized-envelope fallback and observing no change. `.duplication-baseline` re-measured with pylint at both revisions is 35/74/0, matching the committed literal. The lazy `account_helpers` import in `uow.py:150` is load-bearing, reproduced independently by hoisting it. And your refutation of the `_NO_E2E_REST_TAGS` finding is correct: `d912a7ee9` is main's head and this branch's merge-base, so that content is upstream.

The gap is on the other half of the change — see the BLOCKER thread on `account_helpers.py:216`.

### Items with no anchor in this diff

**The description is stale at head in six ways.** (a) "set at the single transport funnel every protocol passes through" — 7 per-tool call sites; raised in round 1 and not corrected. (b) "Deferred paths ... via `account_is_sandbox()`" — now true for 2 of the 7 listed paths. (c) "30 files changed, ~1600 lines" — `git diff --shortstat` gives 33 files, 1898 insertions, 213 deletions. (d) "`tests/unit/` 5573 passed" vs your later 5569; 5569 reproduces. (e) "707 integration" does not reproduce — `pytest tests/integration/` collects 2225 at both revisions, and `tox.ini:68-77` adds only `-m "not skip_ci"`, which cannot account for a 3x gap. (f) the bundled `pyproject.toml` + 300-line `uv.lock` change is not mentioned.

**Two BDD scenarios are dormant, one in no xfail ledger.** `@T-UC-004-identify-account-scope` (`BR-UC-004-deliver-media-buy-metrics.feature:1616`) is XFAIL on all three transports: `Step definition is not found: Given "media buys for "buyer-001" spanning accounts "acct-A" and "acct-B"". Line 1618`. Its Then has no definition either; only the When binds. The tag appears in no xfail registry, so it relies on the auto-xfail hook silently converting the missing step. This is the scenario that would grade `media_buy_delivery.py:260`. A sibling not previously raised, `@T-UC-004-sandbox-natural-key` (`:1626`), is dormant the same way. The exclusion semantic itself **is** graded — `test_sandbox_delivery_account_scoping.py:86-143` covers all three transports with an exact-set assertion and a negative control, and deleting the filter reddens 5 integration tests while reddening zero BDD scenarios. The gap is BDD-specific.

**Two BDD steps that name this PR's invariant cannot see it.** `then_no_sandbox_field` (`then_success.py:122-134`) documents that a `null` counts as present, then reads `model_dump()` — and `_base.py:867-870` forces `exclude_none=True`. A wire probe: mcp carries `"sandbox": null` **on the wire** while the step passes; a2a and rest omit the key, so BR-RULE-209 INV-5 is unenforced on mcp. Separately `then_no_real_api_calls` (`:179-233`) asserts `any(mock.called)` against `EXTERNAL_PATCHES = {"adapter": "...media_buy_delivery.get_adapter"}` — one patch on `get_adapter` itself, so it is True whenever the operation ran, whatever `sandbox=` was passed. Its only discriminating assertion is the unimplemented echo, so once #1874 lands both steps go green while grading nothing about adapter selection. `then_no_billing`'s loop is guarded by `if mock is not None` and never executes. The correct oracle ships in this PR — `assert_all_sandbox`, which refuses an empty call list — with 0 consumers in `tests/bdd/`. Reading `wire_dict(ctx)` (`_outcome_helpers.py:43`), which raises when a real-wire transport did not stash, is the fix for the first.

**Three of five sandbox xfail reasons are false; two entries are unreachable.** Measured by removing each strict xfail under `BDD_ALL_TRANSPORTS=1`, then restoring: `T-UC-004-sandbox-happy` x3 XFAIL -> **FAILED** (`Expected sandbox=True, got None`); `T-UC-005-sandbox-happy` x3 -> **FAILED**; `T-UC-002-sandbox-happy` x3 -> **XFAIL, unchanged**. So your conclusion holds for UC-004/UC-005 and the "stale entries come off" suggestion is falsified there — but the mechanism ("all three route through the inert Given") is wrong for UC-002, which never reaches the Given: an imperative `pytest.xfail()` at `conftest.py:3293` fires during `_harness_env` setup. That also shadows the `_XFAIL_TAGS` entries for UC-002 behind the collection-time `strict=True` marker, so if production catches up nothing will report the XPASS. Reason texts: UC-002's "sandbox mode not implemented in create_media_buy" is stale (`media_buy_create.py:601`, `:3608`); UC-005's is stale (`creative_formats.py:512`/`:521`); UC-004's is half-stale — selection implemented, echo not.

**Two `given_sandbox_account` implementations; 11 of 12 UCs get the inert one.** `given_auth.py:66-71` sets only `ctx["sandbox"]=True`, and `ctx["sandbox"]` has **zero readers** anywhere in `tests/bdd/` (3 grep hits, all writes). The real implementation — `AccountFactory(sandbox=True)` + `AgentAccountAccessFactory` + `_commit_factory_data()` — lives at `uc019_query_media_buys.py:931-957`, absent from `pytest_plugins` and reachable only via `import *` from its own test module. 12 feature files use the phrase across 30 sites; only UC-019 gets the seeding version. Your deferral reason is verified true (30 sites) and is simultaneously the finding. Correctly #1875's scope.

**The delivery-marker deferral rationale is falsified for the case the storyboards exercise.** #1874 §2 says one response can span both modes so a top-level marker is ambiguous. But delivery **does** enrich on all three transports (`:829`, `:885`, `api_v1.py:385`), and this PR consumes the result at `:260` to scope the query to one account — so when `identity.account_id` is set, `set(sandbox_by_buy.values())` has at most one element. `sandbox.mdx:11`: "Sandbox is account-level, not per-request." `get-media-buy-delivery-response.json` has exactly one `sandbox`, at `/properties/sandbox` — no per-buy variant. `delivery_reporting.yaml:191,307` sends `sandbox: true`. The marker is a SHOULD and is storyboard-ungraded (no `check:` on `sandbox` in either scenario's validations), so the deferral is low-risk — and the issue body already states the better rule. Only the summary rationale is weaker than the issue it points at.

**#1874's scope omits two operations this PR sandbox-routes** — `get_media_buys` (`media_buy_list.py:199`) and `update_performance_index` (`performance.py:77`). The issue records why each is out of scope for the marker, which is accurate; the routing/marker distinction is just not drawn.

**Remaining NITs.** The call matcher (`_architecture_helpers.py:170`) is blind to 6 forms — aliased import, a variable holding the function, `functools.partial`, registry dispatch, `getattr`, decorator wrap; 3 confirmed by live mutation. The alias form is realistic here: two functions are named `get_adapter`, and `products.py:707` already carries a disambiguating comment. An empty `KNOWN_EXEMPT` reads as "all caught". The guard's self-test at `:66` re-implements the arm-1 predicate against a synthetic string instead of calling `_get_adapter_calls()`, and covers arm 2 not at all. `uc004_delivery.py:3126`'s `env is None` is structurally unreachable — both call sites are past an earlier `env is None` return at `:3151`. `test_sandbox_delivery_account_scoping.py:191-231` grades adapter mode on mcp only while its sibling class parametrizes all three, and `_ACCOUNT_REF` survives all three. `tests/helpers/__init__.py:86,92-95` re-exports three helpers that 0 of 5 consumers import that way, and `tests/harness/media_buy_account.py:62` sets `last_resolved_sandbox` with no readers. `media_buy_list.py:241`'s new per-buy scoping is graded for single-mode cases but not for the mixed partition that motivated it; its `SNAPSHOT_TEMPORARILY_UNAVAILABLE` fallback is byte-identical on main. Test duplication invisible to the ratchet (it never compares within one module and its threshold is 6 lines): `MethodType(BuyKeyedSandboxMixin.sandbox_mode, uow)` and its twin at 4 sites, each restating the rationale differently; 4 identical env-setup blocks in `test_sandbox_delivery_account_scoping.py` (checked for drift, none found); an 8th copy of an 8-patch scaffold at `test_push_creative_to_existing_buy.py:532-540` where 7 already existed on main. And `media_buy_delivery.py:208-219` / `media_buy_list.py:196-213` remain two implementations of "one adapter per sandbox mode" sharing a byte-identical `get_adapter(...)` tuple. Finally, the executor collapses the typed refusal into `f"Adapter creation failed: {str(e)}"` at `media_buy_create.py:1284` (same at `:1400`), and the admin page renders that refusal as "Delivery metrics are being loaded from the ad server..." (`operations.py:284` + `media_buy_detail.html:159,:172`) — the one case an operator most needs to see, indistinguishable from a transient hiccup.

### Round-1 items, verified at this head

Addressed: `_buy_account_id` deleted (zero hits repo-wide); `:1364` routed through `uow.sandbox_mode_by_id`; `media_buy_update.py:416` through `uow.sandbox_mode(_current_mb)`; `media_buys: Any` resolved by deletion; `repo` renamed to `accounts`; the account scope pushed into `get_by_principal` (mutation-confirmed, 5 failed on removal); `getattr(buy, "account_id", None)` gone; `list[_MediaBuyData]` on both partitions; the guard remediation message corrected. Partial: the `_adapter_for` typing. Declined: `media_buy_create.py:801` (premise falsified) and `operations.py:254` (upheld, reasoning corrected). Tracked: #1874 and #1875, both open and self-assigned — verified, and the standard applied to #1522 in round 1 is met.

### CI

E2E is red at this head, and it is not yours. The same two failures (`test_a2a_protocol_compliance.py::test_get_media_buy_delivery_request_schema`, `test_schema_validation_standalone.py::test_get_products_request_validation`) occur on unrelated PR #1858 @ a0de4e467 with an identical `2 failed, 91 passed, 28 skipped, 4 xfailed`; this diff touches no `tests/e2e/` file and no `adcp_schema_validator.py`; the log shows 302 redirects fetching `https://adcontextprotocol.org/schemas/v1#/...`, a live registry; and PRs #1835/#1806 passed E2E on 2026-08-04, so the break is time-correlated rather than PR-correlated. #1868 exists to pin e2e validation to the installed SDK's 3.1.1 schemas. The other 30 checks pass.

### Not verified

No local full-suite run. Local evidence is scoped: `tests/unit/` (5569) three times, `tests/unit/ tests/harness/` (5706) four times, `tests/integration/` (2225) twice, `tests/bdd/` (1747) once under mutation only, and 521 guard tests. `tests/e2e/` and `tests/admin/` were not run locally. BDD has no clean baseline here, and per-scenario status was read only for the scenarios named above. Whether `GoogleAdManager`/`Kevel`/`TritonDigital` construction performs I/O beyond the credential read was not traced — the `products.py:711` severity rests on `dry_run=True` skipping `GAMClientManager`, which was read but not executed. `media_buy_id` provenance for scanning alert 874 was not traced to the PK column. `uv.lock` was not reviewed beyond the provenance check.
