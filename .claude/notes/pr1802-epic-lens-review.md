# PR #1802 epic lens review — net-deletion + scope
Workflow wf_30e3b178-503 · 19 Fable task reviewers + synthesis · epic salesagent-pldmk
## Answer
Yes, substantially — the epic is deletion-shaped when executed structurally, with two honest exceptions and one gated cluster. Of 19 tasks, 12 are net-deletion, 4 neutral, 2 net-addition, 1 out of scope. The PR-scoped set (15 tasks) deletes: 2 entire guard tests (~500 lines: test_architecture_no_signed_webhook_json_send.py 241, test_architecture_no_laundered_outbound_error.py 259 — their subjects become type errors / structurally unwritable after .1/.3/.9) plus ~548 of 687 lines of the recovery-conformance oracle once .4 removes the ~20 _default_recovery ClassVars it grades, plus 4 planned guards that never get written because the fix makes them pointless; ~30 duplicated src blocks collapsed (the ~40-line send/asend driver twin, 2 delivery twins, 4+2 exception ladders, the ~65-line credential carve-out, WebhookURLValidator's raise→tuple→raise, 2 recovery fallbacks, 11 diff-added asserts, 2 dead destination classes, 4 getattr probes, 2 env-reader copies, _build_url, 3 raw query sites); ~60 test-tier duplicate/fallback sites (13 wire-envelope fallbacks, 2 retry_after tiers, 3 grading ladders, 5-6 scan loops, 4 unsigned-assert copies, ~25 tuple-shape assertions); 3 allowlist/registry entries now (2 no_raw_select, 1 of 6 escape-hatch declarations) with 2 more escape-hatch entries gated behind out-of-PR .7; both duplication baselines ratchet down; ~30 stale citations retired. The honest 'adds only': .17 (one Gherkin line — but it is what keeps 11 already-deleted recovery unit tests deleted) and .15 (~55 lines binding guard subjects to symbols — what keeps .14's citation fixes from rotting). The gated cluster: the two harness assertion helpers and their registry entries need .7's wire projection of the breaker/retry verdict, which is correctly follow-up — so the registry lands at 5/6, not the epic's promised 3/6.

## PR-scoped plan (ordered)
- salesagent-pldmk.1 — first: the typing-and-deletion pass on the seam's own surface closes the boundary the TID251 ban cannot see (types out, not just imports in); root of the graph, unblocks .3 and .13. One correction: no httpx.Timeout on send/asend signatures.
- salesagent-pldmk.9 (absorbing .2 as one change) — second: call_operator_mcp_tool owns dial+retry+extract+both mappings, and the typed unretried re-raise of a wrapped OutboundError is written once inside it, never as .2's standalone carve-out; unblocks .13.
- salesagent-pldmk.3 — after .1: sign-once/measure-once via prepare_signed_request at the top, delete both delivery twins, extract the shared send/asend conclude step; skip the AC's source-diff meta-test option.
- salesagent-pldmk.4 — independent: total recovery table by construction, 3 missing mapping entries, both fallbacks deleted, then ~20 ClassVars, then the ~548-line oracle — strictly in that order.
- salesagent-pldmk.6 — independent, small: repr=False carrier, named outcome constructors at BOTH out-of-module sites, ValueError-narrowed refusal arm; skip the optional construction-site guard.
- salesagent-pldmk.8 (rewritten, deletion core only) — delete the carve-out, WebhookURLValidator, the None overload, the getattr dances, the admin double gate; the moment parameter and error.field unification go to the assigned #1895 follow-up; drop 'closes #1895' from DONE.
- salesagent-pldmk.16 — small boundary pull-in: find_by_url on the repository finishes what the PR did to register_webhook and retires 2 allowlist entries; the webhook_delivery.py update piece is the first thing to drop if trimming.
- salesagent-pldmk.19 — completes the PR's own VendorHttpClient extraction: params+timeout on the frozen client, broadstreet's _build_url and second no-retry statement deleted.
- salesagent-pldmk.5 — test tier: one wire_error_envelope reader with _wire_or_none's contract, underscore-privatize the synthesized field so the tautological `or` is unspellable; execute the credential-refusal parametrize sub-item with judgment or drop it.
- salesagent-pldmk.12 — before any .11 work: measure MCP/REST envelopes for the three T-EGRESS-CREDS scenarios, delete @a2a_untyped_ingest where the triple matches, delete _SINGLE_TRANSPORT_TAGS outright if only a2a-message-send remains.
- salesagent-pldmk.10 (rewritten, doable-now slice only) — delete record_breaker_successes + rewrite the tautological When through env.call_send(); drop the private-range Given and the egress.py registry entry (6→5). Do NOT execute its step 1 as written and do NOT touch the two mixin asserts.
- salesagent-pldmk.13 — after .1/.3/.9: scan_src + assert_delivered_unsigned + lazy tests/helpers __init__, then the delete-or-keep pass retiring the two guards whose subjects the earlier tasks dissolved.
- salesagent-pldmk.17 — one Gherkin recovery line in the live ext-q scenario (mirrored upstream); the behavioral proof that lets the 11 retired recovery tests stay retired. Verify e2e_rest via a bdd-in-network run.
- salesagent-pldmk.14 — citation truth sweep: 21 symbol re-points, CLAUDE.md example, ledger regenerated from baseline files at head, FIXME fixes; prose-only, prefer public-seam citations over the private helper name.
- salesagent-pldmk.15 — last, after .14: derive guard subjects from imported symbols (interpreter as meta-test), CLAUDE.md fence binder test; AC trimmed to the 18 diff-added guards, corpus audit and rename meta-test cut.
- OPTIONAL salesagent-pldmk.7-minimal — if capacity allows: move the four delivery value types into src/core/webhooks/delivery.py, re-point two importers (deleting the PR-created security→persistence inverted import), fold the duplicated conclusion epilogue. Nothing more of .7 in this PR.

## Deferred
- salesagent-pldmk.7 (full) — the notify() unification of five dialect-fork sender sites, breaker relocation and wire projection: the fork sites, the TODOs, and the admin delivery-log gap are all verbatim pre-existing at eccc45a76 (the task's 'added by this diff' claim for creatives.py does not reproduce); rewiring 9+ files of pre-existing webhook structure inside a 32/32-green 333-file PR is the sweep the maintainer warned against. Only the minimal type-move/epilogue slice is PR-introduced.
- salesagent-pldmk.10 (blocked portion) — deleting assert_no_retry_schedule_entered, assert_circuit_breaker_failure_recorded, and their 2 registry entries requires the refusal-verdict wire projection that lands with .7; deleting them now removes live obligations (the head docstring proves delivery_attempts==0 does not carry the retry-schedule obligation). Registry honestly lands at 5/6 in this PR, 3/6 in the follow-up.
- salesagent-pldmk.11 — the transports_for() declarative table restructures four pre-existing registries plus the gate; the one PR-introduced piece (_SINGLE_TRANSPORT_TAGS) is .12's to delete outright. Re-scope .11 after .12's measurement lands: if the registry is deleted, the follow-up shrinks to pre-existing-infra work against a stable base.
- salesagent-pldmk.8 (moment half) — the Literal['registration','dial'] formalization, error.field unification, and package-privatizing EgressPolicy entry points belong to #1895, which is open and assigned; cross-transport error.field changes ripple into BDD envelope assertions and are the destabilizing tail for this PR.
- salesagent-pldmk.18 — out of scope entirely (see rejected); if not closed won't-do, it is a P3 layering follow-up: the tenant-scoped SlackNotifier entry point is a real net-deletion (~25-line session-in-adapter block + 1 allowlist entry) but the PR neither introduced nor multiplied the defect, and doing it now destabilizes the two behaviors this PR just fixed and pinned.

## Rejected / rewrite
- salesagent-pldmk.2 — close as merged into .9, not executed standalone: its entire content (typed unretried re-raise via a generalized find_wrapped) is a ~3-line interior detail of .9's call_operator_mcp_tool; landing it first means writing and then relocating the same fix, churning the chain walker twice. The epic's '.9 needs .2' edge points the wrong way in practice.
- salesagent-pldmk.8 — rewrite as a split: the deletion core (carve-out, WebhookURLValidator, overload, getattr dances, double gate — all diff-added code contradicting the PR's own pinned-schema invariant) goes in this PR; the moment formalization and 'closes #1895' AC are scope creep into an assigned follow-up.
- salesagent-pldmk.10 — rewrite: its step 1 rests on a false premise (assert_no_retry_schedule_entered is NOT redundant with delivery_attempts==0 — the scenario's destination is the metadata address, so origin hits read 0 either way), and its 6→3 registry AC cannot honestly land while .7 carries a FOLLOW-UP disposition. Executed as written it deletes live protection. Keep only the doable-now slice.
- salesagent-pldmk.11 — rewrite as a post-.12 follow-up: the PR-introduced defect (unpinned _SINGLE_TRANSPORT_TAGS above the pinned gate) is dissolved by .12 deleting the registry outright — deleting a mechanism beats declaring-and-pinning it; what remains is a restructure of five pre-existing registries with no production benefit to #1589/#1095/#1441/#1795 and a silent-coverage-drop risk in the exact hook it fixes.
- salesagent-pldmk.18 — close won't-do (or park as backlog): the xandr session-holding block and its allowlist entry are byte-for-byte pre-existing at base; the PR's one touch (base_workflow) converted raw requests.post TO the seam — a strict improvement on the PR's actual claimed invariant. A layering/DRY concern in a different threat model; the issue itself anticipates this disposition.

## Epic corrections
- Dependency '.9 needs .2' is inverted in effect: .2 is a point fix that .9's seam function absorbs. Sequenced as the epic orders it, the fix is written in mcp_client.py and then relocated into call_operator_mcp_tool one task later. Correct move: execute them as one change under .9.
- Task .10 is part-patch as written: its step 1 ('delete assert_no_retry_schedule_entered as redundant') is factually wrong at head, and its 6→3 registry AC smuggles in .7's wire projection while .7 is dispositioned FOLLOW-UP. The '.10 needs .7' edge is correct — the task text just ignores what that gating means for its own acceptance criteria.
- Task .7's premise contains a provenance error: the creatives.py dialect fork, both TODO @yusuf comments, and the admin no-delivery-log gap are verbatim at base eccc45a76, not 'added by this diff'. The task bundles a PR-introduced defect (delivery types parked in the security seam + inverted import + duplicated epilogue — in-scope, small) with pre-existing five-sender structure (follow-up-sized). Its P-level reads as if the whole thing were PR-introduced.
- Task .8's AC 'closes #1895' is scope creep codified into the epic: #1895 is an open, assigned follow-up; the cross-transport error.field ripple is exactly the destabilizing tail the maintainer's FOLLOW-UP disposition already fenced off.
- Task .16 over-counts its own payoff: it promises 3 allowlist retirements; only 2 exist (webhook_delivery.py:425 is a raw update(), which the no-raw-select guard never allowlisted).
- Task .1's AC suggests adding httpx.Timeout to send/asend signatures — that would reopen the exact type boundary the task exists to close; httpx.Timeout belongs only on guarded_async_client, the one acknowledged client-object exemption.
- Task .9's AC counts 'four dials deleted' — there is exactly one injected-client construction at head; what is quadruplicated (and deleted) is the mapping ladder, not the dial.
- Task .15's AC includes a 73-file guard-corpus audit and a rename meta-test — the first is a repo-wide sweep (55 of those guards are pre-existing and untouched; constrain to the 18 diff-added), the second is a guard-on-a-guard that import-resolution makes redundant.
- Task .11's priority misreads its own scope split: the only PR-introduced piece belongs to .12 (which likely deletes it), leaving .11 as pre-existing-infrastructure work misfiled inside a PR-remediation epic.
- Task .17 and .15 should be openly labeled net-addition in the epic — they are, and that is fine: each is the minimal artifact that makes a prior deletion (11 retired tests; string-subject drift) permanent rather than lucky.

## Deletion ledger
- GUARDS — 2 whole guard files deleted via .13 after .1/.3/.9 land in-PR: test_architecture_no_signed_webhook_json_send.py (241 lines; subject becomes a type error once content: bytes lands) and test_architecture_no_laundered_outbound_error.py (259 lines; deleted-or-narrowed once one seam function owns the dialling try). Plus ~548/687 lines of test_architecture_error_recovery_enum_conformance.py (the per-class oracle dies with the ClassVars it grades, .4). Plus 4 planned guards never written: .3's source-diff meta-test, .5's grep-guard, .6's construction-site guard, .15's rename meta-test — each review explicitly rejects the guard because the structural fix subsumes it. Total: ~1,050 guard lines deleted, 4 guards prevented.
- TESTS — 11 retired recovery unit tests STAY retired (.17 is their 4-transport replacement); 13 wire-envelope fallback/accessor sites + 2 of 3 retry_after tiers + 3 per-item grading ladders collapse into one reader (.5); 5-6 hand-rolled src-scan loops, 2 named + 2 inline unsigned-assert copies, and the _serve_in_thread copy collapse into substrate helpers (.13); ~25 TestWebhookURLValidator tuple-shape assertions shrink onto the raising funnel (.8); record_breaker_successes and its tautological When deleted (.10); tests-side .duplication-baseline ratchets down.
- DUPLICATED SRC CODE — ~40 byte-identical send/asend driver lines + both _deliver_signed_webhook twins + the second serialization per delivery (.3); 4 copied except-ladders + 2 three-arm ladders + the type-specific chain walker (.9, absorbing .2); ~65-line credential carve-out + WebhookURLValidator + the None no-op overload + 2 getattr(...,'url') dances + the admin double gate (.8 core); ~20 _default_recovery ClassVars + 2 .get fallbacks + 11 diff-added asserts (.4); 2 dead destination classes + union, 4 getattr probes with the attempts=0→1 silent rewrite, the duplicated env readers, the 3-name status triple, the _refusal detail duplicate (.1); 7 scratch log lines + 2 detail=str(e) sites + the bare except-Exception arm (.6); _build_url (~20 lines) + the twice-stated no-retry rule + broadstreet's direct seam import (.19); 3 raw query sites behind the repository (.16); the duplicate conclusion epilogue + the security→persistence inverted import (.7 minimal slice, optional).
- ALLOWLIST ENTRIES — 2 no_raw_select entries retire now (principals.py register_webhook, webhook_delivery_service.py _send_webhook_enhanced) — NOT the 3 the ticket claims, the raw update() was never allowlisted (.16); 1 of 6 EXPECTED_UNSUPPORTED_DECLARATIONS retires now (egress.py hatch, .10); 2 more (_mixins.py assertion helpers) are gated on .7's wire projection — follow-up; the deleted signed-webhook guard takes its allowlist with it (.13); src-side .duplication-baseline ratchets down (.3 AC).
- STALE CITATIONS / PROSE — 21 webhook_auth_for citation sites re-pointed to the real seam symbol; the dead CLAUDE.md:353 asend example fixed (then executable-bound by .15); the ':157 pre-check' line-number cite replaced by a symbol; 4 wrong Ratchets-ledger numbers + the false 'exactly two' TID251 count regenerated from the tree; the got8 FIXME clause staled by this PR's own capture service re-worded; 1 local-beads-id FIXME re-cited to a GitHub id (.14); the false completeness comment at mcp_client.py:170-181 rewritten (.2/.9); the stale caller docstring at webhook_validator.py:163 deleted (.8).

## Residual risk
After the PR-scoped plan lands: (1) Webhook delivery still has no owning layer above the bytes — five dialect-fork sender sites, per-sender severity choices, and the breaker/retry-schedule facts remain process-local with no wire projection, so two in-process harness asserts and two escape-hatch registry entries (5/6, not 3/6) survive until the .7 follow-up; the admin-originated no-delivery-log gap stays open. (2) BDD transport coverage is still decided by an imperative early-return chain over four pre-existing registries — the invisible-transport-drop defect class that produced #1892 and recurred in this PR remains expressible until .11's table lands post-merge; the .12 measurement may also leave MCP's credential-refusal envelope shape ungraded (FastMCP TypeAdapter divergence), keeping one tag with a recorded reason. (3) The error.field fork stays split until #1895. (4) Credentials remain renderable through the inner pydantic model (r.config) — .6's repr fix protects the carrier only; SecretStr is the deferred escalation. (5) Slack webhook resolution stays session-in-adapter in dead xandr code with its allowlist entry (.18 closed/parked). None of these contradicts the PR's headline guarantees — every outbound request still crosses the one seam — they are the honest edge where this PR's construction ends and the follow-ups begin.
